import { createTheme } from "@mui/material";

export let theme = createTheme({
    palette: {
        primary: {
          main: "rgb(176, 201, 12)",
        },
        secondary: {
          main: "rgb(95, 211, 237)",
        },
      },
})